# CDM (Casanova Drift Model) — Deep Dive
## What it is
A mechanistic spray-drift simulator (v1.2.0) that models the complete physics of pesticide spray droplet transport from nozzle to ground. Originally developed by Ed Casanova (Bayer) and implemented in C++ by John Buonagurio (Exponent). Licensed AGPL-3.0.

## Architecture (C++ modules)
| Module            | File                      | Role                                                                                  |
|-------------------|--------------------------|---------------------------------------------------------------------------------------|
| Public API        | CDM.h                    | C-linkage interface: create model from JSON, run, get output                         |
| Orchestrator      | CDM.cpp                  | Ties all modules together in sequence                                                 |
| Atmospheric       | AtmosphericProperties.cpp| Wet air density, viscosity, dew/wet-bulb temp (Antoine + psychrometric eqns)          |
| Wind Profile      | WindVelocityProfile.cpp  | Logarithmic wind profile fitting, friction velocity, Richardson stability              |
| Nozzle Velocity   | NozzleVelocity.cpp       | Bernoulli → 11 streamline initial velocities                                          |
| Droplet Transport | DropletTransport.cpp     | Core ODE solver — 6-component state vector per droplet                                |
| Deposition        | Deposition.cpp           | Volume distribution → spatial deposition profile                                      |
| DSD Model         | DropletSizeModel.cpp     | Non-linear least squares fit of droplet size distribution                             |
| Serialization     | Serialization.cpp        | JSON ↔ Model struct (nlohmann-json)                                                   |
| CLI               | CDMCLI.cpp               | Command-line driver                                                                   |

## Physics Model (6-DOF ODE per droplet)
The state vector for each droplet has 6 components: [Z, X, Vz, Vx, Mw, Vvwx]
- Gravity + buoyancy — net downward force accounting for air density
- Aerodynamic drag — Clift & Gauvin correlation: CD = 24/Re * (1 + 0.197*Re^0.63 + 0.00026*Re^1.38)
- Droplet evaporation — Ranz-Marshall mass transfer, driven by wet-bulb temperature depression (ΔTwb)
- Wind interaction — log-law wind profile u(z) = (Uf/κ) * ln((z-hC)/z₀), coupled to droplet motion
- Plume spreading — horizontal wind variation (ψψψ) expands the drift footprint crosswind
The ODE is integrated by SUNDIALS CVODE (BDF method, 10000 output steps) until the droplet reaches ground (Z ≤ z₀ + hC) or time runs out.

## Input/Output

**Input:** A JSON file specifying:
- Droplet size distribution (cumulative volume fractions)
- Atmosphere: temperature, pressure, humidity
- Wind: velocity measurements at heights, horizontal variation
- Nozzle: height, pressure, angle, solution properties
- Field geometry: depth, width, nozzle spacing, application rate

**Output:** Deposition curve as [distance (m), % IAR] pairs — how much of the intended application rate lands at each downwind distance. New optional output: vertical drift profiles (% IAR per height bin at specified distances) for NTA/NTTP risk assessment.


```{mermaid}
---
config:
  layout: elk
  elk:
    mergeEdges: true
    nodePlacementStrategy: SIMPLE
---
flowchart TB

    %% ===== INPUT LAYER =====
    subgraph INPUT["Input"]
        direction LR
        I1["Droplet size\ndistribution\n(μm, cumulative\nvolume fraction)"]
        I2["Atmospheric\nconditions\n(T, P, RH)"]
        I3["Wind velocity\nprofile\n(speed & temp\nat heights)"]
        I4["Nozzle\nparameters\n(height, pressure,\nangle)"]
        I5["Solution\nproperties\n(ρW, ρS, xs0)"]
        I6["Field geometry\n(depth, width,\nspacing, IAR)"]
    end

    %% ===== DERIVED PROPERTIES =====
    subgraph DERIVED["Derived Properties"]
        direction LR
        P1["Atmospheric\nproperties\n(ρA, μA, Twb, ΔTwb)"]
        P2["Wind profile\nfitting\n(Uf, z0, ψψψ)"]
        P3["DSD curve fitting\nBi-modal normal\n(μ1, σ1, μ2, σ2, w1)"]
        P4["Nozzle exit velocity\nBernoulli → 11\nstreamline vectors"]
    end

    %% ===== TRANSPORT =====
    subgraph TRANSPORT["Droplet Transport — 6-DOF ODE (CVODE)"]
        direction LR
        T1["Gravity &\nbuoyancy"]
        T2["Aerodynamic drag\nClift & Gauvin"]
        T3["Droplet evaporation\nRanz–Marshall"]
        T4["Wind interaction\nLog-law profile"]
    end

    %% ===== DEPOSITION =====
    subgraph DEPOSITION["Deposition Calculation"]
        direction LR
        D1["Drift distance\nmatrix\n(streamline × size)"]
        D2["Spray volume\ndistribution\n(SVP per class)"]
        D3["Plume spreading\n(ψψψ crosswind)"]
        D4["Spatial integration\nConcentration matrix\n→ % IAR"]
    end

    %% ===== OUTPUT =====
    subgraph OUTPUT["Output"]
        direction LR
        O1["Ground deposition\n% IAR vs distance"]
        O2["Vertical drift profile\n% IAR vs height"]
        O3["Deposition time\n(seconds per droplet)"]
    end

    %% ===== CONNECTIONS =====
    I1 --> P3
    I2 --> P1
    I3 --> P2
    I4 --> P4
    I5 --> P4
    I5 --> TRANSPORT

    P1 --> TRANSPORT
    P2 --> TRANSPORT
    P2 --> D3
    P3 --> D2
    P4 --> TRANSPORT

    I6 --> DEPOSITION

    TRANSPORT --> D1
    TRANSPORT --> O3
    D1 --> D4
    D2 --> D4
    D3 --> D4

    D4 --> O1
    D4 --> O2

    %% ===== STYLING =====
    style INPUT fill:#f5f5f5,stroke:#333,stroke-width:2px
    style DERIVED fill:#e8f4f8,stroke:#5ba4b5,stroke-width:2px
    style TRANSPORT fill:#fff3e0,stroke:#d68f00,stroke-width:2px
    style DEPOSITION fill:#e8f5e9,stroke:#4caf50,stroke-width:2px
    style OUTPUT fill:#f5f5f5,stroke:#333,stroke-width:2px
```

## Dependencies (vcpkg.json)
- SUNDIALS — CVODE ODE solver
- Ceres — non-linear least squares (DSD curve fitting)
- Blaze — matrix math (deposition calculation)
- Boost.Math — root finding, regression, constants
- nlohmann-json — JSON parsing
- fmt — formatted output

## R Package (R/cdm/)

A thin wrapper: the R function cdm(config) calls the C API via .Call('_cdm', ...), returning parsed JSON output as an R list. Includes demo scripts for the 3 SETAC DRAW test cases.

## Sensitivity Analysis (sensitivity/)
A Python script (run_sensitivity.py) that:

1. OAT (one-at-a-time) — perturbs 8 parameters (wind speed, nozzle height/pressure/angle, temperature, humidity, canopy height, wind direction variation) and measures deposition change
2. Parameter sweeps — 15-point response curves for the top 4 most sensitive parameters
3. Two-factor interactions — 8×8 heatmaps for wind×humidity and height×wind
4. Outputs tornado diagrams, response curves, and interaction heatmaps. Calls cdmcli.exe via subprocess.

## Vertical Profile Extension (recent work)

A new capability to extract vertical drift concentration profiles at specified downwind distances — critical for risk assessment of non-target arthropods and terrestrial plants at different canopy heights. Outputs % IAR per height bin (configurable bin width and max height)