image: "gitlab.bayer.com:4567/gbbfx/mondep:latest"

stages:
  - build

build-job:
  stage: build
  script:
    - cmake -B ./build -G Ninja -DCMAKE_BUILD_TYPE=Release -DCMAKE_EXE_LINKER_FLAGS="-static" . && cd ./build && ninja
  artifacts:
    paths:
      - ./bin/cdm